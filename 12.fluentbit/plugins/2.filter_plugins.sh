Purpose: Filter plugins modify, enrich and drop records based on specific rules.

1. Grep plugin -  Filters records by matching a regualr expression
2. Modify plugin - Adds, removes and modifies fields in the log records
3. Parser plugin - Parses specific fields in the logs

